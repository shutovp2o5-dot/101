# 🔧 Исправление проблемы с подключением бота

## Проблема
Бот не может подключиться к Telegram API из-за блокировки TCP соединения на порт 443.

## Решения

### 1. Проверка файрвола macOS

Выполните в терминале:
```bash
# Проверить статус файрвола
sudo /usr/libexec/ApplicationFirewall/socketfilterfw --getglobalstate

# Если файрвол включен, временно отключите для теста:
sudo /usr/libexec/ApplicationFirewall/socketfilterfw --setglobalstate off

# Или добавьте Python в исключения:
sudo /usr/libexec/ApplicationFirewall/socketfilterfw --add /usr/bin/python3
sudo /usr/libexec/ApplicationFirewall/socketfilterfw --unblockapp /usr/bin/python3
```

### 2. Проверка системных настроек

1. Откройте **Системные настройки** → **Безопасность** → **Файрвол**
2. Если файрвол включен:
   - Либо отключите его временно для теста
   - Либо добавьте Python в список разрешенных приложений

### 3. Проверка VPN/Прокси

Если вы используете VPN или прокси:
- Попробуйте временно отключить VPN
- Проверьте настройки прокси в системных настройках

### 4. Проверка интернет-соединения

```bash
# Проверка интернета
curl -I https://google.com

# Проверка подключения к Telegram API
curl -I https://api.telegram.org
```

### 5. Перезапуск сетевых служб

```bash
# Перезапуск Wi-Fi
sudo networksetup -setairportpower en0 off
sleep 2
sudo networksetup -setairportpower en0 on
```

## После исправления

1. Запустите проверку подключения:
```bash
cd ~/Desktop/task-manager-bot
python3 check_connection.py
```

2. Если все проверки пройдены, перезапустите бота:
```bash
cd ~/Desktop/task-manager-bot
./restart_bot.sh
```

3. Проверьте работу бота, отправив команду `/start` в Telegram

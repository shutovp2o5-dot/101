# 🎤 Настройка Replicate API для распознавания голосовых сообщений

## Что такое Replicate?

Replicate - это платформа, которая предоставляет доступ к различным ML моделям, включая Whisper для транскрипции аудио. Это отличная альтернатива OpenAI API, часто дешевле и проще в использовании.

## Преимущества Replicate API

- ✅ Дешевле, чем OpenAI API
- ✅ Использует WhisperX (улучшенная версия Whisper)
- ✅ Хорошее качество распознавания
- ✅ Поддержка русского языка
- ✅ Простая настройка

## Шаги настройки

### 1. Получите API токен

1. Перейдите на https://replicate.com
2. Зарегистрируйтесь или войдите в аккаунт
3. Перейдите в **Account** → **API Tokens**
4. Скопируйте ваш API токен (или создайте новый)

### 2. Создайте файл .env

В папке бота создайте или отредактируйте файл `.env`:

```bash
cd ~/Desktop/task-manager-bot
echo "REPLICATE_API_TOKEN=ваш_токен_здесь" >> .env
```

**Важно:** Замените `ваш_токен_здесь` на ваш реальный токен из Replicate.

### 3. Установите библиотеку replicate

```bash
pip install replicate
```

Или если используете requirements.txt:
```bash
pip install -r requirements.txt
```

### 4. Перезапустите бота

```bash
./restart_bot.sh
```

## Готово! ✅

Теперь отправьте голосовое сообщение боту - оно должно распознаться через Replicate API.

## Проверка настройки

Проверьте, что всё настроено правильно:

```bash
cd ~/Desktop/task-manager-bot

# Проверка токена в .env
grep REPLICATE_API_TOKEN .env && echo "✅ Токен найден в .env" || echo "❌ Токен не найден"

# Проверка библиотеки
python3 -c "import replicate; print('✅ replicate установлена')" 2>&1 || echo "❌ replicate не установлена"
```

## Стоимость

Replicate взимает плату за использование моделей, но обычно это дешевле, чем OpenAI:
- Примерно $0.001-0.005 за минуту аудио (зависит от модели)
- Для голосовых сообщений это очень дешево

## Используемая модель

Бот использует модель **keeandev/whisperx** - это улучшенная версия Whisper с:
- Word-level timing
- Лучшим качеством распознавания
- Поддержкой русского языка

## Альтернативы

Если Replicate не подходит, вы можете использовать:
- **OpenAI Whisper API** - лучшее качество, но дороже
- **Google Speech Recognition** - бесплатно, но требует установки библиотек

## Устранение проблем

Если распознавание не работает:

1. Проверьте логи бота:
   ```bash
   tail -f bot_run.log | grep -E "(Replicate|распознано|Ошибка)"
   ```

2. Убедитесь, что токен правильный в `.env` файле

3. Проверьте, что библиотека установлена:
   ```bash
   pip install replicate
   ```

4. Перезапустите бота после изменений

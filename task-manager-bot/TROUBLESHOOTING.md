# 🔧 Устранение проблем с ботом

## Бот не запускается или не отвечает

### 1. Проверьте, что бот запущен

Откройте терминал и выполните:
```bash
cd ~/Desktop/task-manager-bot
python3 bot_advanced.py
```

Вы должны увидеть:
```
🔍 Проверка подключения к Telegram API...
🤖 Улучшенный бот для управления проектами и задачами запущен!
📡 Подключение к Telegram API...
🎤 Поддержка голосовых сообщений включена!
```

### 2. Проверьте токен

Убедитесь, что файл `.env` содержит правильный токен:
```bash
cat ~/Desktop/task-manager-bot/.env
```

Должно быть:
```
TELEGRAM_BOT_TOKEN=8331027176:AAGsD7L-riQxPd4UnQS9lf_ddFn5qcAI0Rs
```

### 3. Проверьте зависимости

```bash
cd ~/Desktop/task-manager-bot
pip3 install -r requirements.txt
```

### 4. Проверьте интернет-соединение

```bash
ping api.telegram.org
```

Если пинг не проходит, настройте DNS (см. инструкцию выше).

### 5. Проверьте, не запущен ли другой экземпляр бота

```bash
ps aux | grep bot_advanced.py
```

Если видите запущенные процессы, остановите их:
```bash
pkill -f bot_advanced.py
```

### 6. Проверьте логи ошибок

Если бот запускается, но не отвечает, проверьте вывод в терминале на наличие ошибок.

## Частые ошибки

### Ошибка: "TELEGRAM_BOT_TOKEN не указан"

**Решение:** Проверьте файл `.env` в папке с ботом.

### Ошибка: "NetworkError" или проблемы с DNS

**Решение:** Настройте DNS серверы (8.8.8.8 и 1.1.1.1) в системных настройках.

### Ошибка: "Conflict: другой экземпляр бота уже запущен"

**Решение:** Остановите все запущенные экземпляры бота:
```bash
pkill -f bot_advanced.py
```

### Ошибка: "ModuleNotFoundError"

**Решение:** Установите зависимости:
```bash
pip3 install -r requirements.txt
```

## Как правильно запустить бота

1. Откройте терминал
2. Перейдите в папку:
   ```bash
   cd ~/Desktop/task-manager-bot
   ```
3. Запустите бота:
   ```bash
   python3 bot_advanced.py
   ```
4. Оставьте терминал открытым (бот должен работать постоянно)
5. Проверьте в Telegram - отправьте `/start`

## Проверка работы бота

1. Бот должен быть запущен в терминале
2. В Telegram найдите вашего бота
3. Отправьте команду `/start`
4. Бот должен ответить приветственным сообщением

## Если ничего не помогает

1. Перезагрузите компьютер
2. Проверьте интернет-соединение
3. Убедитесь, что токен правильный
4. Попробуйте запустить бота заново

# 🎤 Настройка распознавания голосовых сообщений

## Проблема: голосовые сообщения не распознаются

Для работы распознавания голосовых сообщений нужно настроить один из методов:

## Метод 1: OpenAI Whisper API (Рекомендуется - лучшее качество)

### Установка:
1. Получите API ключ на https://platform.openai.com/api-keys
2. Установите переменную окружения:
   ```bash
   export OPENAI_API_KEY='ваш_ключ_здесь'
   ```
3. Или добавьте в файл `.env`:
   ```
   OPENAI_API_KEY=ваш_ключ_здесь
   ```

### Преимущества:
- ✅ Лучшее качество распознавания
- ✅ Поддержка русского языка
- ✅ Работает с любыми голосовыми сообщениями

## Метод 2: Google Speech Recognition (Бесплатный)

### Установка:
```bash
pip install SpeechRecognition pydub
brew install ffmpeg  # для macOS
```

### Преимущества:
- ✅ Бесплатный
- ✅ Не требует API ключа
- ⚠️ Требует интернет-соединение
- ⚠️ Может быть менее точным чем OpenAI

## Быстрая установка всех зависимостей:

```bash
cd ~/Desktop/task-manager-bot
pip install -r requirements.txt
brew install ffmpeg  # для macOS
```

## Проверка работы:

1. Отправьте голосовое сообщение боту
2. Бот должен распознать текст и продолжить создание задачи
3. Если не работает - проверьте логи: `tail -f bot_run.log`

## Устранение проблем:

### Ошибка: "speech_recognition не установлена"
```bash
pip install SpeechRecognition pydub
```

### Ошибка: "ffmpeg не найден"
```bash
# macOS
brew install ffmpeg

# Linux
sudo apt-get install ffmpeg

# Windows
# Скачайте с https://ffmpeg.org/download.html
```

### Ошибка: "Не удалось распознать речь"
1. Проверьте интернет-соединение
2. Говорите четче и медленнее
3. Установите OPENAI_API_KEY для лучшего качества
4. Проверьте логи бота

## Рекомендации:

Для лучшего качества используйте OpenAI Whisper API:
- Регистрация: https://platform.openai.com
- Получите API ключ
- Добавьте в `.env` файл: `OPENAI_API_KEY=ваш_ключ`

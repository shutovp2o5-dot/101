# 🚀 Команды для управления ботом

## Быстрый старт

### Перезапуск бота
```bash
cd ~/Desktop/task-manager-bot
./restart_bot.sh
```

### Остановка бота
```bash
cd ~/Desktop/task-manager-bot
./stop_bot.sh
```

### Проверка статуса
```bash
cd ~/Desktop/task-manager-bot
./status_bot.sh
```

## Доступные скрипты

### `restart_bot.sh` - Перезапуск бота
Останавливает старый процесс и запускает новый:
- Останавливает все запущенные процессы бота
- Проверяет DNS
- Запускает бота в фоновом режиме
- Показывает статус запуска

**Использование:**
```bash
./restart_bot.sh
```

### `stop_bot.sh` - Остановка бота
Останавливает все процессы бота:
- Находит и останавливает процессы бота
- При необходимости выполняет принудительную остановку

**Использование:**
```bash
./stop_bot.sh
```

### `status_bot.sh` - Проверка статуса
Показывает текущий статус бота:
- Проверяет, запущен ли бот
- Показывает информацию о процессе (PID)
- Выводит последние строки лога
- Проверяет DNS

**Использование:**
```bash
./status_bot.sh
```

### `START_BOT.sh` - Запуск бота (интерактивный)
Запускает бота в интерактивном режиме (не в фоне):
- Останавливает старые процессы
- Запускает бота в текущем терминале
- Можно остановить через Ctrl+C

**Использование:**
```bash
./START_BOT.sh
```

## Полезные команды

### Просмотр логов в реальном времени
```bash
tail -f bot_run.log
```

### Просмотр последних 50 строк лога
```bash
tail -50 bot_run.log
```

### Поиск ошибок в логе
```bash
grep -i error bot_run.log
```

### Проверка запущенных процессов
```bash
ps aux | grep bot_advanced.py
```

### Ручная остановка бота
```bash
pkill -f bot_advanced.py
```

### Ручной запуск бота
```bash
cd ~/Desktop/task-manager-bot
python3 bot_advanced.py > bot_run.log 2>&1 &
```

## Примеры использования

### Полный цикл перезапуска
```bash
cd ~/Desktop/task-manager-bot
./stop_bot.sh      # Остановить
sleep 2            # Подождать
./restart_bot.sh   # Запустить заново
```

### Проверка работы после перезапуска
```bash
./restart_bot.sh
sleep 5
./status_bot.sh
```

### Мониторинг бота
```bash
# В одном терминале запускаем бота
./restart_bot.sh

# В другом терминале смотрим логи
tail -f bot_run.log
```

## Устранение проблем

### Бот не запускается
1. Проверьте статус: `./status_bot.sh`
2. Проверьте логи: `tail -50 bot_run.log`
3. Проверьте DNS: `ping api.telegram.org`
4. Проверьте токен: `cat .env`

### Бот запущен, но не отвечает
1. Проверьте логи: `tail -f bot_run.log`
2. Проверьте интернет-соединение
3. Перезапустите: `./restart_bot.sh`

### Несколько процессов бота
```bash
# Остановить все процессы
pkill -9 -f bot_advanced.py

# Запустить заново
./restart_bot.sh
```

# 🔄 Инструкция по перезапуску бота

## Быстрый перезапуск

### Вариант 1: Bash скрипт (рекомендуется)
```bash
cd ~/Desktop/task-manager-bot
./restart_bot_improved.sh
```

### Вариант 2: Python скрипт
```bash
cd ~/Desktop/task-manager-bot
python3 restart_bot.py
```

### Вариант 3: Старый скрипт (если нужен)
```bash
cd ~/Desktop/task-manager-bot
./restart_bot.sh
```

## Дополнительные команды

### Остановить бота
```bash
# Bash
./stop_bot.sh

# Python
python3 restart_bot.py --stop
```

### Только запустить бота
```bash
# Bash
./START_BOT.sh

# Python
python3 restart_bot.py --start
```

### Проверить подключение
```bash
python3 restart_bot.py --check
# или
python3 check_connection.py
```

### Просмотр логов
```bash
# В реальном времени
tail -f bot_run.log

# Последние 50 строк
tail -50 bot_run.log

# Поиск ошибок
grep -i error bot_run.log
```

### Проверка статуса бота
```bash
ps aux | grep bot_advanced.py
```

## Что делают скрипты

1. **Останавливают** все запущенные процессы бота
2. **Проверяют** подключение к Telegram API
3. **Запускают** бота в фоновом режиме
4. **Сохраняют** логи в `bot_run.log`
5. **Показывают** статус запуска

## Устранение проблем

Если бот не запускается:

1. Проверьте токен:
   ```bash
   cat .env | grep TELEGRAM_BOT_TOKEN
   ```

2. Проверьте подключение:
   ```bash
   python3 check_connection.py
   ```

3. Проверьте логи:
   ```bash
   tail -50 bot_run.log
   ```

4. Проверьте файрвол (см. FIX_CONNECTION.md)

# 🎤 Настройка распознавания голосовых сообщений

## Текущая ситуация

Транскрипция Telegram может быть недоступна в Bot API или в вашем регионе. Для распознавания голосовых сообщений нужно настроить один из внешних методов.

## ✅ Рекомендуемое решение: OpenAI Whisper API

**Лучшее качество распознавания, поддерживает русский язык**

### Шаги настройки:

1. **Получите API ключ:**
   - Перейдите на https://platform.openai.com/api-keys
   - Войдите или зарегистрируйтесь
   - Создайте новый API ключ (Secret key)

2. **Создайте файл `.env` в папке бота:**
   ```bash
   cd ~/Desktop/task-manager-bot
   echo "OPENAI_API_KEY=ваш_ключ_здесь" >> .env
   ```
   
   Замените `ваш_ключ_здесь` на ваш реальный API ключ.

3. **Перезапустите бота:**
   ```bash
   ./restart_bot.sh
   ```

4. **Готово!** Теперь отправьте голосовое сообщение боту.

### Преимущества:
- ✅ Высокое качество распознавания
- ✅ Поддержка русского языка
- ✅ Работает надежно
- ✅ Не требует установки дополнительных библиотек

### Стоимость:
- OpenAI Whisper API платный, но очень дешевый
- Примерно $0.006 за минуту аудио
- Для голосовых сообщений это копейки

## Альтернатива: Google Speech Recognition

**Бесплатно, но требует установки библиотек**

### Шаги настройки:

1. **Установите библиотеки:**
   ```bash
   pip install SpeechRecognition pydub
   ```

2. **Убедитесь, что установлен ffmpeg:**
   ```bash
   brew install ffmpeg  # для macOS
   ```

3. **Перезапустите бота:**
   ```bash
   ./restart_bot.sh
   ```

### Преимущества:
- ✅ Бесплатно
- ✅ Хорошее качество для русского языка

### Недостатки:
- ⚠️ Требует интернет-соединение
- ⚠️ Может быть медленнее, чем OpenAI

## Быстрая проверка

После настройки проверьте:

```bash
# Проверка OpenAI API ключа
cd ~/Desktop/task-manager-bot
grep OPENAI_API_KEY .env && echo "✅ OpenAI API настроен" || echo "❌ OpenAI API не настроен"

# Проверка библиотек
python3 -c "import speech_recognition; print('✅ speech_recognition установлена')" 2>&1 || echo "❌ speech_recognition не установлена"
```

## Что делать прямо сейчас?

**Рекомендую настроить OpenAI Whisper API** - это самый надежный способ:

1. Получите ключ на https://platform.openai.com/api-keys
2. Создайте `.env` файл с ключом
3. Перезапустите бота
4. Готово!

## Если ничего не работает

Используйте **текстовые сообщения** - они работают всегда и не требуют настройки! 📝

# 🚀 Быстрое исправление DNS

## Проблема
Бот не может подключиться к Telegram API из-за проблем с DNS.

## ✅ Решение 1: Через hosts файл (быстро)

Выполните в терминале:
```bash
sudo sh -c 'echo "149.154.167.220 api.telegram.org" >> /etc/hosts'
```

Затем запустите бота:
```bash
cd ~/Desktop/task-manager-bot
python3 bot_advanced.py
```

## ✅ Решение 2: Настройка DNS серверов (надежно)

Выполните в терминале:
```bash
sudo networksetup -setdnsservers Wi-Fi 8.8.8.8 1.1.1.1
```

Или через системные настройки:
1. Системные настройки → Сеть
2. Выберите подключение → Дополнительно → DNS  
3. Добавьте: `8.8.8.8` и `1.1.1.1`
4. Применить

## ✅ Проверка

После исправления проверьте:
```bash
ping -c 1 api.telegram.org
```

Если пинг проходит - DNS исправлен!

## 🚀 Запуск бота

```bash
cd ~/Desktop/task-manager-bot
python3 bot_advanced.py
```

Вы должны увидеть:
```
✅ DNS работает корректно
🤖 Улучшенный бот для управления проектами и задачами запущен!
📡 Подключение к Telegram API...
```

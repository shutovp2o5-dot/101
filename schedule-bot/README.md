# 🤖 Telegram Бот для Управления Расписанием

Telegram бот для удобного управления личным расписанием прямо в мессенджере.

## ✨ Возможности

- ✅ Добавление событий с указанием даты, времени и описания
- ✅ Редактирование существующих событий
- ✅ Удаление событий
- ✅ Просмотр всех событий
- ✅ Фильтрация событий (сегодня, неделя)
- ✅ Категории событий (Работа, Личное, Учёба, Здоровье, Другое)
- ✅ Удобный интерфейс с кнопками
- ✅ Сохранение данных в JSON файле

## 📋 Требования

- Python 3.8 или выше
- Telegram аккаунт
- Токен бота от @BotFather

## 🚀 Установка и Настройка

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Создание бота в Telegram

1. Откройте Telegram и найдите бота [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и создайте бота
4. Сохраните полученный токен (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 3. Настройка токена

Установите токен как переменную окружения:

**Linux/macOS:**
```bash
export TELEGRAM_BOT_TOKEN='ваш_токен_здесь'
```

**Windows (PowerShell):**
```powershell
$env:TELEGRAM_BOT_TOKEN='ваш_токен_здесь'
```

**Windows (CMD):**
```cmd
set TELEGRAM_BOT_TOKEN=ваш_токен_здесь
```

Или создайте файл `.env` (не забудьте добавить его в `.gitignore`):
```
TELEGRAM_BOT_TOKEN=ваш_токен_здесь
```

### 4. Запуск бота

```bash
python bot.py
```

Бот запустится и будет готов к работе! Найдите вашего бота в Telegram и отправьте команду `/start`.

## 📖 Использование

### Основные команды:

- `/start` - Начать работу с ботом
- `/help` - Показать справку
- `/add` - Добавить новое событие
- `/list` - Показать все события
- `/today` - Показать события на сегодня
- `/week` - Показать события на неделю

### Добавление события:

1. Отправьте команду `/add`
2. Введите название события
3. Введите дату в формате `ДД.ММ.ГГГГ` (например: `25.12.2024`)
4. Введите время в формате `ЧЧ:ММ` (например: `14:30`)
5. Введите описание (или `/skip` чтобы пропустить)
6. Выберите категорию из предложенных

### Управление событиями:

- После просмотра списка событий (`/list`) вы можете выбрать любое событие
- Для каждого события доступны кнопки:
  - ✏️ **Редактировать** - изменить событие
  - 🗑️ **Удалить** - удалить событие

## 📁 Структура проекта

```
telegram-schedule-bot/
├── bot.py              # Основной код бота
├── requirements.txt     # Зависимости Python
├── README.md           # Документация
└── schedule_data.json  # Файл с данными (создаётся автоматически)
```

## 🔧 Развертывание для постоянной работы

⚠️ **Важно:** Если бот запущен локально на вашем компьютере, он перестанет работать при выключении компьютера. Для постоянной работы бота его нужно развернуть на облачном сервисе или сервере.

### 🚀 Быстрый старт (Railway - рекомендуется)

Самый простой способ развернуть бота:

1. **Создайте аккаунт на [Railway](https://railway.app)** (войдите через GitHub)
2. **Создайте новый проект** → "Deploy from GitHub repo"
3. **Выберите ваш репозиторий** с ботом
4. **Добавьте переменную окружения:**
   - `TELEGRAM_BOT_TOKEN` = ваш токен бота
5. **Готово!** Бот запустится автоматически

Railway предоставляет **500 бесплатных часов в месяц** - этого достаточно для постоянной работы бота.

### 📚 Подробные инструкции

Полные инструкции по развертыванию на разных платформах смотрите в файле **[DEPLOYMENT.md](DEPLOYMENT.md)**:

- ✅ **Railway** - самый простой вариант (рекомендуется)
- ✅ **Render** - альтернативный облачный сервис
- ✅ **Docker** - для самостоятельного сервера
- ✅ **systemd** - для Linux сервера

### 💻 Локальный запуск (для тестирования)

Для тестирования на локальном компьютере:

```bash
python bot.py
```

Или в фоне с screen:

```bash
screen -S telegram-bot
python bot.py
# Нажмите Ctrl+A, затем D для отсоединения
```

## 🛠️ Разработка

Бот написан на Python с использованием библиотеки `python-telegram-bot`.

Основные компоненты:
- **ConversationHandler** - для многошаговых диалогов
- **InlineKeyboard** - для удобных кнопок
- **JSON файл** - для хранения данных

## 📝 Лицензия

Свободное использование для личных целей.

## 🤝 Поддержка

Если возникли проблемы:
1. Проверьте, что токен установлен правильно
2. Убедитесь, что установлены все зависимости
3. Проверьте версию Python (должна быть 3.8+)

---

Создано с ❤️ для эффективного управления временем в Telegram

# Объединенный Telegram бот: Расписание + Задачи

Этот бот объединяет функционал двух ботов:
1. **Расписание** - бот для управления расписанием событий
2. **Задачи** - бот для управления задачами и проектами

## Структура

- `unified_bot.py` - основной файл объединенного бота
- `.env` - файл с токеном бота (скопирован из task-manager-bot)

## Установка и запуск

1. Убедитесь, что у вас установлены все зависимости из обоих ботов
2. Убедитесь, что файл `.env` содержит токен: `TELEGRAM_BOT_TOKEN=ваш_токен`
3. Запустите бота:
   ```bash
   python3 unified_bot.py
   ```

## Использование

1. Отправьте команду `/start` боту
2. Вы увидите главное меню с двумя кнопками:
   - **📅 Расписание** - переключение в режим расписания
   - **✅ Задачи** - переключение в режим задач
3. После выбора режима откроется соответствующее меню с функционалом выбранного бота
4. Для возврата в главное меню используйте кнопку "🏠 Главное меню" (если доступна)

## Текущие ограничения

⚠️ **ВНИМАНИЕ**: Полная интеграция обработчиков требует дополнительной настройки:
- ConversationHandler из обоих ботов требуют специальной маршрутизации
- Некоторые обработчики могут конфликтовать между режимами
- Рекомендуется тестирование перед использованием в продакшене

## Структура данных

Бот использует те же файлы данных, что и оригинальные боты:
- `schedule_data.json` - данные расписания (из бота расписания)
- `tasks_data.json` - данные задач (из бота задач)

## Разработка

Для добавления новых обработчиков:
1. Убедитесь, что обработчик проверяет режим через `context.user_data.get('bot_mode')`
2. Используйте обертки `create_schedule_wrapper()` или `create_tasks_wrapper()` для маршрутизации
3. Регистрируйте обработчики после загрузки модулей
